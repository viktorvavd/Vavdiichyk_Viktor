import genereteMessage.RandomMessage;
import product.Product;
import repository.Storage;

import java.util.LinkedList;
import java.util.List;

public class Main {
    public static void main(String[] args) {
        try {
            Thread.sleep(100);
        } catch (InterruptedException e) {
            e.printStackTrace();
        }
    }
}
